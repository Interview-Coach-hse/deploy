apiVersion: v1
kind: Service
metadata:
  name: {{ include "interview-coach-backend.fullname" . }}
  labels:
    {{- include "interview-coach-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "interview-coach-backend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
