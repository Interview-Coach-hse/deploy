apiVersion: v1
kind: Service
metadata:
  name: {{ include "interview-coach-frontend.fullname" . }}
  labels:
    {{- include "interview-coach-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "interview-coach-frontend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
